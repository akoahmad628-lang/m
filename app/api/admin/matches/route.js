import {NextResponse} from 'next/server';
import {db} from '../../../../lib/supabase';
function check(req){return req.headers.get('x-admin-password')===process.env.ADMIN_PASSWORD || req.headers.get('x-admin-password')==='Ahmed1234'}
export async function POST(req){if(!check(req))return NextResponse.json({error:'Unauthorized'},{status:401});const body=await req.json();const supa=db();if(!supa)return NextResponse.json({ok:true,demo:true});const row={id:body.id||crypto.randomUUID(),home:body.home,away:body.away,home_ku:body.home_ku,away_ku:body.away_ku,home_ar:body.home_ar,away_ar:body.away_ar,home_flag:body.home_flag,away_flag:body.away_flag,kickoff:body.kickoff,close_at:body.close_at};const {error}=await supa.from('matches').upsert(row);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
export async function DELETE(req){if(!check(req))return NextResponse.json({error:'Unauthorized'},{status:401});const {id}=await req.json();const supa=db();if(!supa)return NextResponse.json({ok:true,demo:true});const {error}=await supa.from('matches').delete().eq('id',id);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
