# Marina World Cup 2026

Clean Next.js website.

## Deploy
1. Upload all files/folders inside this folder to GitHub.
2. Import the GitHub repo in Vercel.
3. Deploy.

## Pages
- `/` public prediction page
- `/admin` admin control
- `/winners` past winners

Admin password is inside `app/admin/page.js`. Change it there before deploying if needed.
